name: mukhla_splitw
description: MUKHLA - Kharche Pai Charcha - Premium Expense Tracker

publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Suite (Updated to Resolvable versions)
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  firebase_messaging: ^16.1.0
  firebase_storage: ^13.0.5

  # Google Sign In
  google_sign_in: ^7.2.0

  # State Management
  provider: ^6.1.1

  # UI Components
  cupertino_icons: ^1.0.6

  # Utils
  intl: ^0.20.2
  uuid: ^4.3.3

  # Share & Utils
  share_plus: ^12.0.1
  fluttertoast: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true